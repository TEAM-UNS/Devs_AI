"""기술스택 추출 — 섹션 분할 + 별칭 매칭. taxonomy.yaml 사전 사용.

단계
    1. 본문을 자격요건 / 우대사항 / 주요업무 / 복지 로 분할
    2. 복지 섹션 제외 (복리후생의 브랜드명이 스킬로 오탐되는 것 방지)
    3. 별칭 사전 매칭 (소문자·공백제거 정규화 후 비교)
    4. 등급 부여
         자격요건 → required
         우대사항 → preferred
         사이트 제공 태그(원티드·점핏) → tag
    5. 모호 스킬(Go · C · R)은 taxonomy 의 requires 문맥 단서가 없으면 미채택

연봉 파싱도 여기서 담당한다.
    "3,000~4,000만원"    → range
    "2,600만원 이상"      → min_only
    "회사내규에 따름" · "면접 후 결정" → negotiable
    그 외 파싱 불가       → unknown (금액 컬럼 null)
"""
